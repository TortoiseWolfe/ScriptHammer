# Primer: a historic-rebuild modelling session

Paste everything below the line into a new Claude Code session opened in `~/repos/ScriptHammer`. It is plain paragraphs on purpose, because markdown travels with a paste. Change the building name in the second paragraph when you move past the Car Barns.

---

I am re-modelling, by hand in SketchUp, the historic buildings of the ScriptHammer chatt twin, replacing the 3D Warehouse models it borrows (issue 714 explains the licence problem). Read docs/twins/chatt-historic-rebuild.md first; it is the plan, and it has the licence rules, the data sources, the start points and the replace-a-slot recipe. The ranked list of buildings, each with its sources and the Warehouse slug it replaces, is docs/twins/chatt-historic-backlog.md. The epic is issue 1289.

This session is about the Chattanooga Car Barns, issue 1290: the 1886-87 stable and car barn at the SW corner of Market and W 3rd, CARTA's bus garage until 1978, demolished from July 2025. Its brief is docs/twins/car-barns/BRIEF.md and its LiDAR measurements are docs/twins/car-barns/massing_spec.json. Where the two disagree, the spec wins. The research archive (NRHP photos, the 1975 plan, Sanborn sheets, LiDAR tiles and the scripts that measured them) is on the Windows side at C:\Users\JonPo\Downloads\SketchUpMake2017\reference\chatt-historic-rebuild-2026-09-23\research. Read the issue body first, and check off what is already done before starting anything.

The SketchUp pipeline lives in my private repo ~/repos/ada-stair-generator. Before touching it, run git status and git worktree list there. Another session may hold the primary checkout. Work in your own worktree off main, never in the primary checkout. The Car Barns site is data/sites/chattanooga-car-barns (read its README). Its carbarns_scaffold step built the starter model C:\Users\JonPo\Downloads\SketchUpMake2017\car-barns\cb001-scaffold.skp; every later build gets the next cbNNN name. Its tests are tests/test_car_barns_scaffold.rb. That repo's GitHub Actions do not start (a billing block on the private repo), so run its tests in Docker, on ruby:3.2-slim and on ruby:2.2-slim (SketchUp's Ruby). Read its CLAUDE.md and docs/house/ runbooks for how sites, gates and spawn-point waypoints work. For a demolished building the Street View stages do not apply; the cameras come from the archival photos instead, controlled by LiDAR corners.

Rules that are easy to break. Geometry comes only from public-domain sources: USGS LiDAR, Library of Congress Sanborn maps, NAIP and USGS aerials, HABS, and my own photos. Everything else (the NRHP photos, Wikimedia Commons, Flickr, county and TDOT imagery, OpenStreetMap) is for measuring only, never tracing or texturing. Google Street View, Google Earth and satellite imagery, SketchUp Photo Textures, and anything from 3D Warehouse stay out of the modelling session entirely. Never put my email address in any request or User-Agent; use ScriptHammer-research/1.0 with the repo URL. Never print anything from a .env. Docker-first for everything in ScriptHammer; commit hooks are never skipped.

What I want from this session: tell me in two or three sentences where the Car Barns work stands, and what the next step is. Name the decisions still open for me: the era (October 1978 is recommended), the modelling tool given the SketchUp Make 2017 licence, the output licence, and the credit name. Then do the next step. If the next step is a SketchUp run, give me the exact commands and the file it will write, and never overwrite an existing model or a spawn copy.

When a step finishes, rewrite the issue body (the body is the finding; comments are history), and correct the backlog or the brief wherever they turned out wrong. Keep the twin's walk spawn at Market and W 3rd unless I say otherwise.
